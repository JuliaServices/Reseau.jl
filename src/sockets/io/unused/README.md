# `src/sockets/io/unused/`

Code in this directory is not currently included by `src/sockets/sockets.jl` and is not part of the runtime package.

It is kept for parity reference and may not compile or pass tests until it is re-enabled deliberately.

