name: CI
on:
  push:
    branches: [main, master]
    tags: ["*"]
  pull_request:
  workflow_dispatch:
jobs:
  test:
    name: Julia ${{ matrix.version }} - ${{ matrix.os }} - ${{ matrix.arch }} - ${{ github.event_name }}
    runs-on: ${{ matrix.os }}
    timeout-minutes: 5
    env:
      RESEAU_RUN_TLS_TESTS: ${{ matrix.run_tls_tests }}
      RESEAU_RUN_NETWORK_TESTS: ${{ matrix.run_network_tests }}
      RESEAU_TRIM_RUN_EXE_WINDOWS: "1"
      RESEAU_TRIM_DEBUG: "1"
      RESEAU_TRIM_ONLY: "eventloops_trim_safe.jl"
      RESEAU_TRIM_EXE_TIMEOUT_S: "30.0"
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            arch: x64
            version: '1.12'
            run_tls_tests: '1'
            run_network_tests: '1'
    steps:
      - uses: actions/checkout@v4
      - uses: julia-actions/setup-julia@v2
        with:
          version: ${{ matrix.version }}
          arch: ${{ matrix.arch }}
      - uses: julia-actions/julia-buildpkg@v1
      - name: Run trim debug workload
        run: julia --project=. --color=yes --check-bounds=yes --depwarn=yes --startup-file=no test/trim_compile_tests.jl
